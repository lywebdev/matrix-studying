<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing "<%= tweet.title %>"</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
<div id="app">
    <div class="container" style="width: 700px; margin: 0 auto;">
        <h1>Editing "<%= tweet.title %>"</h1>
        <form action="/tweets/update/<%= tweet.id %>" method="POST" class="form">
            <div>
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" required minlength="20" maxlength="50" value="<%= tweet.title %>">
            </div>
            <div style="margin-top: 5px;">
                <label for="tweet">Tweet:</label>
                <input type="text" id="tweet" name="tweet" required maxlength="50" value="<%= tweet.tweet %>">
            </div>
            <button type="submit" style="margin-top: 20px;">Update tweet!</button>
        </form>

        <a href="/" class="link" style="margin-top: 10px;">Go to home</a>
    </div>
</div>
</body>
</html>